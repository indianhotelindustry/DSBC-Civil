{
  "name": "dsbc-functions",
  "description": "DSBC Civil — server-authoritative financial Cloud Functions",
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --format=esm --outfile=lib/index.js --external:firebase-functions --external:firebase-admin",
    "deploy": "npm run build && firebase deploy --only functions",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^8.3.2",
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^6.1.1",
    "node-cron": "^4.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
